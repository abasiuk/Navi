<UserControl x:Class="Navi.View.ClientPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Navi.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="0,0,-269,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Пошук клієнта" Grid.Row="0" FontSize="20" Foreground="#FF3AFF43" HorizontalAlignment="Left" Margin="5,0,0,0"/>
        <TextBox Text="{Binding Pattern, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Width="300" VerticalAlignment="Center" HorizontalContentAlignment="Center" HorizontalAlignment="Left" VerticalContentAlignment="Center" Padding="0,4" Margin="5,10,0,10" FontSize="16"/>
        <ListBox Grid.Row="2" Margin="5" MaxHeight="150"
             ItemsSource="{Binding Strings}"
             SelectedItem="{Binding Selected}" SelectionChanged="OnSelectionChanged" HorizontalAlignment="Left" Width="300">
        </ListBox>
    </Grid>
</UserControl>